name: owntend
description: "Supabase-backed home asset maintenance manager. Everything you own. Well tended."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+2

environment:
  sdk: ^3.13.0
  flutter: ">=3.47.0"

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  cupertino_icons: ^1.0.9
  flutter_riverpod: ^3.4.2
  go_router: ^17.5.0
  drift: ^2.34.3
  drift_flutter: ^0.3.1
  path_provider: ^2.1.6
  path: ^1.9.1
  uuid: ^4.6.0
  intl: ^0.20.3
  collection: ^1.19.1
  flutter_secure_storage: ^11.0.0
  flutter_local_notifications: ^22.3.0
  flutter_foreground_task: ^11.0.1
  permission_handler: ^13.0.1
  workmanager: ^0.10.9
  fl_chart: ^1.2.0
  image_picker: ^1.2.3
  share_plus: ^13.3.0
  file_picker: ^12.0.0
  archive: ^4.1.0
  crypto: ^3.0.7
  http: ^1.6.0
  sqlite3: ^3.5.2
  timezone: ^0.11.1
  material_symbols_icons: ^4.2951.0
  geolocator: ^14.0.3
  audioplayers: ^6.8.1
  google_sign_in: ^7.2.0
  google_mobile_ads: ^9.1.0
  supabase_flutter: ^2.17.2
  connectivity_plus: ^7.3.0
  package_info_plus: ^10.2.1
  sentry_flutter: ^9.27.0
  shorebird_code_push: ^2.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_native_splash: ^2.4.8

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of the
  # package. See that file for information on deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  build_runner: ^2.16.0
  drift_dev: ^2.34.5
  mocktail: ^1.0.5
  path_provider_platform_interface: ^2.1.3
  coverage: ^1.15.1
  sentry_dart_plugin: ^3.4.0

sentry:
  upload_debug_symbols: true
  upload_sources: false
  upload_source_maps: false
  symbols_path: build/sentry-debug/dart
  dart_symbol_map_path: build/sentry-debug/dart/mapping.json
  wait_for_processing: true
  log_level: error

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  default-flavor: dev
  generate: true

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons.
  uses-material-design: true

  assets:
    - assets/audio/
    - assets/brand/google-g-logo.png
    - assets/illustrations/
    - assets/splash/owntend_splash_icon_3d.png

  fonts:
    - family: Geist
      fonts:
        - asset: assets/fonts/Geist-Regular.ttf
          weight: 400
        - asset: assets/fonts/Geist-Medium.ttf
          weight: 500
        - asset: assets/fonts/Geist-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/Geist-Bold.ttf
          weight: 700

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_hurr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/assets-and-images#from-packages

  # To add custom fonts to your application, add a fonts section in this
  # "flutter" section. Each entry should have a "family" key and a "fonts"
  # list with asset paths and optional descriptors.

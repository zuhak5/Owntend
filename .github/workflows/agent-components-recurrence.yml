name: Agent TaskCard recurrence localization

on:
  push:
    branches:
      - agent/fix-data-contract-and-arabic-localization

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out remediation branch
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
        with:
          fetch-depth: 0

      - name: Apply TaskCard recurrence localization
        run: python tool/agent_components_recurrence.py

      - name: Remove temporary automation
        run: |
          rm tool/agent_components_recurrence.py
          rm .github/workflows/agent-components-recurrence.yml

      - name: Check patch hygiene
        run: git diff --check

      - name: Commit and push recurrence fix
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Fix TaskCard Arabic recurrence"
          git push origin HEAD:${GITHUB_REF_NAME}

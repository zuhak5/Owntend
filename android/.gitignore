# Gradle wrapper bootstrap files are tracked and integrity-checked
# (see config/toolchain.json gradleWrapperJarSha256 and WP-002 policy).
# Only build state and local properties stay ignored.
/.gradle
/captures/
/local.properties
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks

# Generated by Flutter for the current dependency/build mode. Keeping this
# out of source control prevents test-only plugins leaking into release builds.
app/src/main/java/io/flutter/plugins/GeneratedPluginRegistrant.java
